import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Cat Clicky Game!</h1>
        <p>Click on a cat to earn points, but don't click on a cat more than once!</p>
    </div>
);


export default Title;

